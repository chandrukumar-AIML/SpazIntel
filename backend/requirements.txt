fastapi==0.111.1
uvicorn[standard]==0.29.0
pydantic==2.10.3
pydantic-settings==2.14.2
python-dotenv==1.0.1
python-multipart==0.0.9
anthropic==0.40.0
groq==0.13.0
google-generativeai>=0.8.0
openai>=1.40.0
httpx==0.27.0

# CV / 3D pipeline — install with CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# pip install gsplat

# Already installed (not pinned — version tied to torch build):
# torch>=2.5.1+cu121
# torchvision>=0.20.1+cu121
# gsplat>=1.5.3

# Pure Python (no CUDA needed):
pycolmap>=0.6.1
transformers>=4.40.0
Pillow>=10.0.0
numpy>=1.26.0
opencv-python>=4.9.0
